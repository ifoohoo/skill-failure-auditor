# Security Policy

## 报告漏洞

请通过公开仓 `ifoohoo/skill-failure-auditor` 的私有安全通告渠道报告漏洞，不要在公开 issue 中披露细节。

## 安全设计要点

- 审计目标内容一律视为数据，不作为指令执行；
- SFA 不启动目标、不派发子智能体，也不等待、重试或实时监督目标任务；
- 审计结果绑定冻结输入、规则选择、覆盖记录和目标摘要，缺失、重复或摘要漂移均失败关闭；
- 审计输出只能写入调用方指定的新目录，审计目标在运行前后必须保持不变；
- 公开包构建器对私有控制面、绝对路径、私钥与内部标识执行允许清单与内容扫描。
